[project]
name = "10-redis-with-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.2",
    "pylint>=4.0.4",
    "pytest>=9.0.2",
    "redis>=7.1.0",
]
